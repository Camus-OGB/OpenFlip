# Port d'écoute (défaut: 8000)
PORT=8000

# Taille max des fichiers en bytes (défaut: 50MB)
MAX_FILE_SIZE=52428800

# Base de données - PostgreSQL (Production) ou SQLite (Local)
# Pour la production sur Render, vous recevrez automatiquement cette URL
# Laissez cette ligne commentée en développement local
# DATABASE_URL=postgresql://user:password@host:5432/dbname
